# TradePilot AI — Python backend dependencies.
# Populated incrementally as functionality is built.

# Milestone 3 — database layer
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0
pytest>=7.4

# Milestone 4 — backend API
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Milestone 5 — chart capture (LIVE mode only; DEMO mode needs none of this)
playwright>=1.40
